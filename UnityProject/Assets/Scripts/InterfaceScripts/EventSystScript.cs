using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class EventSystScript : MonoBehaviour
{
    // Start is called before the first frame update
    public static EventSystScript instance;

    private void Awake()
    {
       
    }
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
